<% if(erros.length > 0) {%>
    <% erros.forEach(function(er) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <%= er.msg %>
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    <% });%>
<%} %>